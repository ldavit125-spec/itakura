# 📄 개발 인계 가이드 (HANDOVER_GUIDE.md)

집이나 다른 개발 환경(Codex 등)에서 작업을 연속해서 진행하실 때 참고하실 수 있도록 **핵심 주의사항, 주요 작업 내역, 개발 시작 절차**를 정리한 문서입니다.

---

## 1. 깃허브 브랜치 및 원격 저장소 정보

* **작업 브랜치**: `feature/japanese-ui`
* **목표 브랜치**: `main`
* **깃허브 저장소**: `https://github.com/ldavit125-spec/itakura.git`

---

## 2. 작업 환경 세팅 및 시작 명령어

다른 환경이나 집에서 작업을 이어받을 때 다음 명령어를 차례대로 실행하세요.

```bash
git fetch origin
git checkout feature/japanese-ui
git pull origin feature/japanese-ui
npm install
npm run dev
```

---

## 3. 핵심 도메인 규칙 및 절대 주의사항 ⚠️

1. **이임원 관리자 이름 유지 (최우선 규칙)**:
   * 일본어 모드(`ja`) 전환 시에도 이임원 관리자의 이름은 일본어로 번역하거나 가타카나 변환하지 않고 **한국어 원본 그대로 `이임원`**으로 유지해야 합니다.
2. **날짜 입력 필드 (`DateInput`) 규칙**:
   * `YYYY-MM-DD` 키보드 직접 입력 지원
   * invalid 날짜 입력 시 빨간 테두리 시각적 검증 처리
   * `Enter` 키 입력 또는 Focus `Blur` 시 값 확정(commit)
   * 📅 달력 선택 연동 규칙 유지
3. **완제품 LOT 자동 연동**:
   * 생산관리에서 생산 완료된 모든 제품은 품질관리 및 재고의 **완제품 LOT** 탭에 자동으로 즉시 연동/표시되어야 함.
4. **품질관리 & 생산관리 레이아웃 통일성**:
   * 메인 카드 상단 영역의 이중 회색 서브 바(`bg-gray-50`)를 사용하지 않고 메인 카드로 깔끔하게 통합.
   * 신규 등록 버튼(`+ 신규 검사 등록`, `+ 원재료 입고검사 등록`, `+ 공정검사 등록` 등)은 필터 바의 **우측 끝**에 배치하는 디자인 파라미터 유지.
5. **다국어 (i18n) 적용 표준**:
   * 하드코딩된 한국어 텍스트 금지. `useLanguage()`의 `t()` 함수 및 `localizedName({ locale, ko, ja })` 헬퍼 함수 활용.
   * 일본어 전환 시 모든 표 헤더, 상태 카운트(`現在 稼働/待機 作業: N件`), 단위(`件`, `個`, `単位`), 모달 필드(예: `計画開始時間`, `計画終了時間`, `新規検査登録`)가 자연스러운 일본어로 렌더링되어야 함.

---

## 4. 최근 주요 구현 내역 요약

1. **품질관리 레이아웃 개편**: 생산관리 UI 구조와 100% 디자인 통일.
2. **품질검사 > 신규 검사 등록 모달 (`InspectionQueueCreateModal`)**:
   * 원재료 입고검사 / 공정검사 / 완제품검사 구분 선택
   * 원재료(입고 LOT), 공정(생산 LOT), 완제품(완제품 LOT) 자동 연동 선택
   * `REQ-YYYYMMDD-XXX` 요청번호 및 요청일시 자동 채번
   * 일본어 전환 시 완벽한 일본어 렌더링 (`新規検査登録`)
3. **생산관리 (작업상황) 다국어 완전 보완**:
   * `現在 稼働/待機 作業: N件`
   * `予定開始`, `実績開始`, `現在生産量`
   * `master.field.unit` (`単位`), `計画開始時間`, `計画終了時間` 누락 번역 키 복구 및 적용

---

## 5. 작업 완료 시 검증 및 푸시 절차

새로운 작업을 완료한 후에는 반드시 프로덕션 빌드 검증을 진행해야 합니다.

```bash
# 1. 빌드 및 타입 검사 (에러 0건 확인)
npm run build

# 2. 변경 내역 확인 및 푸시
git status
git add .
git commit -m "feat: [작업 내용 요약]"
git push origin feature/japanese-ui
```
